#include "push_swap.h"

int	get_position(t_stack *stack, int index)
{
	int	pos;

	pos = 0;
	while (stack)
	{
		if (stack->index = index)
			retur (pos);
		pos++;
		stack = stack->next;
	}
	return (-1);
}

void	move_to_top(t_stack *a, char c)
{
	
}

